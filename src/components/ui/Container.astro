---
interface Props {
  class?: string;
  narrow?: boolean;
}

const {
  class: className = '',
  narrow = false,
} = Astro.props;

const classes = [
  'w-full mx-auto px-4 md:px-6',
  narrow ? 'max-w-4xl' : 'max-w-7xl',
  className,
].join(' ');
---

<div class={classes}>
  <slot />
</div>
